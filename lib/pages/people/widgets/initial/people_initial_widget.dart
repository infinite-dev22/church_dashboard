import 'package:flutter/cupertino.dart';

class PeopleInitialWidget extends StatelessWidget {
  const PeopleInitialWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Your people appear here"),
    );
  }
}
